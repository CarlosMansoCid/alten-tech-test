import { memo } from "react";
import SearchBoxContainer from "../../common/containers/search-box.container";

const MainPageSearchBoxContainer = () => {
  return <SearchBoxContainer />;
};

export default memo(MainPageSearchBoxContainer);
