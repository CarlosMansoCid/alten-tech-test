import { memo } from "react";
import SearchBoxContainer from "../../common/containers/search-box.container";

const FavoritesPageSearchBoxContainer = () => {
  return <SearchBoxContainer />;
};

export default memo(FavoritesPageSearchBoxContainer);
